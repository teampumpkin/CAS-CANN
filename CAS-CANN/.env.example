# ================================================
# CAS Website - Environment Configuration Template
# ================================================
# Copy this file to `.env` and fill in your actual values
# NEVER commit the `.env` file to version control!

# ================================================
# Node Environment
# ================================================
NODE_ENV=production
PORT=5000

# ================================================
# Database Configuration (PostgreSQL)
# ================================================
# Format: postgresql://username:password@host:port/database
# Example: postgresql://cas_user:secure_password@localhost:5432/cas_db
DATABASE_URL=postgresql://username:password@host:port/database

# ================================================
# Zoho CRM Integration
# ================================================
# Get these from Zoho API Console: https://api-console.zoho.com
ZOHO_CLIENT_ID=1000.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
ZOHO_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ZOHO_ORG_ID=900159811

# Optional: Custom OAuth scopes (comma-separated)
# Default: ZohoCRM.modules.ALL,ZohoCRM.settings.ALL
# ZOHO_OAUTH_SCOPES=ZohoCRM.modules.ALL,ZohoCRM.settings.ALL

# ================================================
# Session & Security
# ================================================
# Generate a strong random secret for session encryption
# Use: openssl rand -base64 32
SESSION_SECRET=your-super-secure-random-session-secret-here

# API key for automation endpoints (change in production!)
AUTOMATION_API_KEY=your-automation-api-key-change-in-production

# ================================================
# Email Notifications (Optional)
# ================================================
# SMTP Configuration for email notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password

# Email notification settings
# NOTIFICATION_EMAILS=admin@amyloid.ca,leanne@amyloid.ca
# NOTIFICATIONS_ENABLED=true
# WEEKLY_REPORTS_ENABLED=true
# REAL_TIME_ALERTS_ENABLED=false

# ================================================
# Replit-Specific (Auto-set by Replit)
# ================================================
# These are automatically set when deployed on Replit
# You don't need to set them manually on Replit
# REPLIT_DEPLOYMENT=1
# REPL_ID=your-repl-id

# ================================================
# Production Deployment Notes
# ================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in all required values (marked with XXXXXX)
# 3. Generate SESSION_SECRET: openssl rand -base64 32
# 4. Verify DATABASE_URL connection works
# 5. Test Zoho credentials at: /oauth/zoho/debug
# 6. NEVER commit .env to Git (it's in .gitignore)
